# ============================================================
# Ansible Configuration — NextStep Local
# ============================================================

[defaults]
inventory = inventory.ini
remote_user = vagrant
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
